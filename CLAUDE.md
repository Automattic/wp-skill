<!--
  Agent instructions for this repo live in AGENTS.md (agent-agnostic — Codex et al. read it
  natively). Claude Code does NOT auto-read AGENTS.md, so this one-line bridge imports it.
  Per-developer machine-specific notes go in a gitignored AGENTS.local.md (imported below if
  present; absent for most clones, which is fine).
-->
@AGENTS.md
@AGENTS.local.md
